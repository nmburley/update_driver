# GitHub Actions workflow: run updating_both_drivers.pl on Windows and Linux when code changes
# Place this file at .github/workflows/update-drivers.yml in your repo.

name: update drivers (windows + linux)

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch: {}

jobs:
  run-update-script:
    strategy:
      matrix:
        include:
          - os: windows-selfhost
            # Use the labels that match your self-hosted runner.
            # Common labels: self-hosted, Windows, X64
            # Or use a custom label you assigned like "local-win"
            runs-on: [self-hosted, Windows]
          - os: ubuntu-latest
            runs-on: [self-hosted, Linux, X64]
      fail-fast: false

    # Use the runs-on value from the matrix (string or array of labels)
    runs-on: ${{ matrix.runs-on }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Print runtime info
        run: |
          echo "Runner OS: $RUNNER_OS"
          perl -v || echo "perl not found; expected to be available on runner"

      - name: Run updating_both_drivers.pl
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          perl .\updating_both_drivers.pl

      - name: Run updating_both_drivers.pl (Linux)
        if: runner.os == 'Linux'
        run: |
          perl ./updating_both_drivers.pl

      - name: Upload artifacts (optional)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: driver-output-${{ matrix.os }}
          path: |
            windows/**/*
            linux/**/*
            ./*.zip
            ./*.log
